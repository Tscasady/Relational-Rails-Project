<h1>All Players</h1>

<a href="/players/new">New Player</a>

<% @players.each do |player| %>
  <h3><%= link_to "#{player.name}", "/players/#{player.id}" %> <%= player.created_at %></h3>
  <%= link_to "Edit #{player.name}", "/players/#{player.id}/edit" %>
<% end %>